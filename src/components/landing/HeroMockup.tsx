import styles from './hero.module.css';

// Placeholder — will be redesigned in next phase
export default function HeroMockup() {
  return (
    <div className={styles.mockupWrap} aria-hidden="true">
      {/* Mockup content coming soon */}
    </div>
  );
}
